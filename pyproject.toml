[project]
name = "chartroom"
version = "0.1.0"
description = "Credentialed proxy for CHARTROOM. The gap engine runs in the browser."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "websockets>=13",
]

[tool.uv]
package = false
